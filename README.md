# Filmhouse

Promotional site for Filmhouse, a monthly film club.

Static HTML — no build step. Open `index.html` in a browser to preview locally.

## Deploy

The site deploys to Cloudflare Pages on every push to `main`.
